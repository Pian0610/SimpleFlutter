specVersion: "v1beta"
serviceId: "basic_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "basic_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
